# Claude Instructions for Perl Core

Use `AGENTS.md` as required context for all tasks in this repository.

Repository-specific skill files are located in `perl_agents/skills`. Read the
relevant `SKILL.md` file before working on the matching subsystem, and follow
its referenced files as needed.

The `perl_agents/skills` tree is the legacy mirror of the canonical skill
source in `../perl/.agents/skills`. There is no separate
`.claude/skills` mirror in this repository.

## Companion instructions

This file is a concise pointer; see `AGENTS.md` for the full repository
guidance and make changes there instead.
